
# READ BEFORE CONTINUE!

> [!TIP]
> _Use kali linux or any type of operating system ( Windows/Linux ), not recommended to use Android, even as a root user._

Recommend using the Python, the nodejs version is not working properly or stable and the nodejs version is still the old version and not updated yet cuz i am too lazy to do it. just use the Python. 

The nodejs version is unstable.
- sometimes not showing the menu 
- error while reading bruteforce command ( cannot bruteforce the wifi ssid )
- slow response/respond ( can take more than 20 second after running 1 command, like usage or scan

what is shs?
shs is a tool that can bruteforce or the simple way to say it "force connect" to a wifi.
